🔥 브랜치 나눠서 작업할 때 절대 하면 안 되는 것
❌ main에서 직접 코드 수정

→ 충돌 지옥

❌ 음악 듣기 버튼 만들면서 로그인 기능도 섞어서 Commit

→ 기능마다 커밋 분리해야 추적 가능

❌ 하나의 feature 브랜치로 모든 기능 개발

→ PR이 너무 커짐 → 리뷰가 불가능해짐



# 브랜치 생성
git checkout -b feature/login

# 작업, 커밋
git add .
git commit -m "로그인 기능 구현"

# develop 최신 코드 가져오기
git checkout develop
git pull origin develop

# 다시 내 feature 브랜치로 돌아가기
git checkout feature/login
git merge develop  # 충돌 확인 후 해결

# 작업이 끝나면 push
git push origin feature/login
